# App Store metadata

Canonical metadata for Prism Roll 1.1.0, app ID `6809253424`.

Validate locally with `asc metadata validate --dir metadata` and review a remote plan with `asc metadata apply --app 6809253424 --app-info 73758c1f-c1f8-484a-a7be-fa2a8cdeac30 --version 1.1.0 --dir metadata --dry-run` before applying. The app-info ID is the editable 1.1.0 draft, not the live 1.0.0 record.

Screenshots and videos are generated from actual simulator captures using `ios/scripts/compose_growth_screenshots.py` and `ios/scripts/compose_growth_video.py`. September 2026 capture sources, exported assets, and upload evidence are local under `ios/release/growth-2026-09-16/` (ignored build/release artifacts).
