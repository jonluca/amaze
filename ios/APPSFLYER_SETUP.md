# AppsFlyer free setup

Status on September 20, 2026: the owner completed signup, the email confirmation link was followed, and the signed-in account is **Active** on the **Welcome Plan**. Prism Roll: Paint Maze Puzzle is registered as iOS app `id6809253424`, live in the store, with USD reporting and UTC timezone. The issued SDK developer key is stored privately. The Payments & invoices page explicitly shows **no payment method**; no paid upgrade or advertising campaign was enabled. AppsFlyer accepted both a real SDK session and a fresh QA install after initial HTTP errors. The refreshed Marketing Overview for Prism Roll on September 20 UTC shows **1 Organic install**, corresponding to internal QA, not customer acquisition. Active users and revenue still show **No data found**. No new App Store binary has been released in this setup.

## Free plan boundary

New accounts receive the Welcome package: up to 12,000 measured conversions or one year, whichever comes first, plus 30 days of selected premium add-ons. Without adding billing or upgrading, the account transitions to Zero. Zero remains free but measures clicks and impressions rather than install attribution. The actual account dashboard displays **September 21, 2026 through September 20, 2027**, **12,000 remaining units**, and zero conversions at setup. Selected premium add-ons display an end date of **October 20, 2026**. These are the account's displayed dates, not dates inferred from the local signup day. Do not build a permanent dependency on the temporary raw-data API trial.

Sources: [plan and billing details](https://support.appsflyer.com/hc/en-us/articles/115000136303-Account-management-plans-payments-and-billing), [pricing](https://www.appsflyer.com/pricing/).

## Account activation

1. Complete [free signup](https://www.appsflyer.com/start/zero/) without a payment method. The owner must create the password and accept the terms.
2. Verify the account shows Welcome/free and no billing method or paid add-ons.
3. Register the published iOS app **Prism Roll**, Apple ID **6809253424**, bundle **com.jonluca.prismroll**. Use its actual App Store listing and verify the resulting app record.
4. Obtain the account's SDK developer key. It is distinct from an administrative API token; basic SDK initialization does not require generating an API token or granting a new integration account.
5. Supply the real developer key as the `APPSFLYER_DEV_KEY` Xcode build setting through the [private build configuration](Config/README.md). `Config/Local.xcconfig` is ignored and owner-readable only; normal local Debug/Release builds resolve the issued key, while clean checkouts/CI and explicit empty overrides remain keyless. Confirm the resulting built app's `PrismAppsFlyerDevKey` is populated without printing it into logs. Do not use a placeholder key or claim a keyless build is connected.
6. Confirm the prepared privacy policy and App Store disclosures match the final native configuration before distributing an enabled binary.
7. On a designated test installation, verify the opt-in and opt-out UI, real session receipt in the correct AppsFlyer app, lifecycle behavior and no new requests after withdrawal. Separate QA traffic from acquisition reports. SDK compilation and mocked-transport tests cannot prove provider receipt.

## Integration boundary

The implementation uses AppsFlyer's Strict iOS SDK to omit advertising-identifier code. It additionally disables vendor-identifier collection, SKAdNetwork activity, Apple Ads attribution, TCF collection and downstream partner sharing. Firebase retains its separate Apple Ads integration. It does not forward customer identifiers, contact details, Game Center identities, shared-maze URLs, purchase transactions or ad revenue. Google Analytics remains responsible for the existing gameplay and revenue reporting.

AppsFlyer still processes an installation identifier and app/device/network/session information after affirmative consent. This is not anonymous aggregate-only collection. Existing consent that named only Google must not silently authorize AppsFlyer. Existing users need a new attribution choice; a configured new installation can disclose both providers in its initial choice. The attribution choice also requires usage analytics to be enabled. Ordinary Debug, XCTest and UI-test runs remain unable to transmit. Missing configuration leaves AppsFlyer inactive without disabling Google Analytics.

Withdrawal prevents the app from starting further AppsFlyer sessions and uses the SDK stop control. It does not delete prior provider reports or reset the AppsFlyer installation identifier. Firebase's existing identifier-reset behavior remains separate. Re-enabling attribution must not issue duplicate starts within the same foreground cycle. Strict SDK 7.0.2 has a stale public `disableSKAdNetwork` getter: exact device and simulator binary review shows the setter updates the configuration used by the actual SKAN controller/timer gates, while the getter reads another byte. Keep the supported setter and plist; do not add a private-API workaround. The diagnostic therefore labels this value as the public getter, not proof of the effective setting. This review does not establish the absence of all server-side settings traffic. Verify stop/resume network behavior against the exact pinned SDK: the generic online documentation and version-specific header describe parts of that behavior differently.

The committed source configuration is intentionally keyless; this local checkout has the issued key in its ignored private configuration. Registering an account or adding the SDK does not alter the currently released App Store binary; enabling the integration for customers requires a new release.

## Disclosure review before release

The app manifest now additionally declares **Other Data Types → Analytics, linked, not tracking**, based on AppsFlyer's documented technical/network and installation information. Existing categories remain unchanged. The owner restored App Store Connect browser authentication, and the equivalent answers were prepared and retained locally for the next enabled release. The live form's final action was **Publish**, with no separate Save action; it was canceled. Reload confirmed the existing publication and all **10 data types** were unchanged, with no saved App Store Connect draft. Apple requires responses for an available app to describe the data collected by that version; live **1.1.0 (build 15)** does not include AppsFlyer. Coordinate publication of the prepared answers with the AppsFlyer-enabled release, following [Apple's App Privacy guidance](https://developer.apple.com/help/app-store-connect/manage-app-information/manage-app-privacy). No App Store privacy publication was changed.

The classification is source/documentation-based; it is not a decoded payload inventory. Review the final archive's combined privacy report before distribution. The AppsFlyer account shows zero active partner integrations, Aggregated Advanced Privacy on, view-through probabilistic modeling off, IP masking on, and re-engagement/uninstall measurement off. These settings were read back after saving.

The [public policy](https://playprismroll.com/privacy) now names AppsFlyer, the new consent control, the data boundary and withdrawal limitations, conditional on versions supporting the integration. Existing Google Analytics, Crashlytics, advertising and Game Center explanations remain intact. Website commit `5b7a36a32b67c6ad25eae287beb18a095f3f19c8` deployed successfully; both public policy URLs returned HTTP 200 and matched the committed bytes. No enabled app binary has been distributed yet.

## Attribution links

OneLink template `i67L` is configured on `playprismroll.onelink.me`. Its generated test link is explicitly labeled **Prism Roll — setup QA**, media source `prismroll_qa`, campaign `setup_20260920`. iOS redirects to the App Store; Android and desktop use the App Store website. Custom website fallback showed an allowlist warning and was not retained. No payment method was added to unlock redirect features. The browser client blocked the QA link navigation, so end-to-end redirection is not claimed. The public website continues using its existing Apple campaign links until the SDK-enabled app is released and validated. No ad campaign or retargeting was enabled.

The existing daily acquisition monitor also checks meaningful AppsFlyer quota/expiry changes and reporting readiness while preserving the no-billing and no-campaign boundary.

## Local validation

The keyless app and tests compiled with Strict SDK **7.0.2**. All **22 focused tests** passed: 10 attribution consent/readiness cases, 7 existing analytics cases, and 5 purchase-revenue policy cases. They cover old consent migration, both required choices, withdrawal, saved preferences, ordinary Debug/test suppression, absent/template configuration, independent Firebase behavior, and stale readiness after withdrawal/re-enable. Test configuration values only reach injected recorders; they are never used with the real SDK. Swift formatting and lint checks passed.

The subsequent diagnostics/configuration work passed **17 keyless and 17 configured consent tests** and Swift quality checks. Actual Xcode Debug and Release settings resolved the issued key; an explicit empty override and absent private include remained keyless. The designated QA app showed the combined first-run explanation, kept AppsFlyer uninitialized for decline and Google-only consent, and initialized only with both choices. Withdrawal called stop and both choices stayed off across a cold launch, with no new AppsFlyer host requests in the observed post-withdrawal window.

Two initial requests returned SDK error 40 and correlated endpoint HTTP 404. With the same app/key configuration, a session reached the launches endpoint with HTTP 200 at **23:31:28 UTC**. Because a session does not prove a first install was recorded, one new isolated simulator was used: its first affirmative choice produced **conversions HTTP 200 at 23:34:27 UTC**. These are internal QA events, not customer acquisition. SDK readbacks matched the issued key and numeric app ID and confirmed IDFV/Apple Ads/partner controls; the SKAN getter exception is documented above. An attribution lookup still returned 404, so no paid-campaign or link-attribution result is claimed. A subsequent refreshed Marketing Overview for Prism Roll, filtered to September 20 UTC, showed **1 Organic install** in both its metric and chart. This verifies install-report receipt for internal QA; it does not establish session reporting or customer acquisition. Active users and revenue remained **No data found**. Dashboard evidence is `release/analytics/appsflyer-2026-09-20/dashboard-login-followup.txt`.

These local runs used the existing unrelated working-tree Google Mobile Ads **13.10.0** upgrade, which is not part of the AppsFlyer commits. No release archive, production upload, physical-device test, decoded payload inventory, paid attribution, or live resume-acceptance result is claimed. Evidence is under `release/analytics/appsflyer-2026-09-20/native-live/`; the initial keyless log/result are `/tmp/PrismRollAppsFlyerConsent.log` and `/tmp/PrismRollAppsFlyerConsent.xcresult`.

Implementation references: [Strict SDK installation](https://dev.appsflyer.com/hc/docs/install-ios-sdk-7), [initialization and session lifecycle](https://dev.appsflyer.com/hc/docs/integrate-ios-sdk-7), [privacy controls](https://dev.appsflyer.com/hc/docs/preserve-user-privacy-ios-7), [AppsFlyer services privacy policy](https://www.appsflyer.com/legal/services-privacy-policy/).
